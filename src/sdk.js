import { Meraki } from '@/Meraki';
import { MerakiScript } from '@/MerakiScript';
import * as utils from '@/Utilities';

export const sdk = {
    Meraki,
    MerakiScript,
    utils,
};

export default sdk;
